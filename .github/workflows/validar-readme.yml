name: validad-readme

on:
  push:
  push_request:

jobs:
 validar:
  runs-on: ubuntu-latest
  steps:
   - name: Descargar codigo
     uses: actions/checkout@v4

    - name: Validad README
      run: test -f README.md

   - name:

